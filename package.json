{
  "name": "pronto-proxy",
  "version": "0.0.0",
  "scripts": {
    "build": "docker build --platform linux/amd64,linux/arm64 -t mtobisch/pronto-proxy:1.0.4 -t mtobisch/pronto-proxy:latest .",
    "push": "docker push mtobisch/pronto-proxy:1.0.4 && docker push mtobisch/pronto-proxy:latest"
  },
  "private": true,
  "author": "",
  "license": "ISC"
}
